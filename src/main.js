// TODO(you): import the Three.js library so we can use it below
// hint: `import * as THREE from 'three'`
// notice: no './' in front — that's for local files, but 'three' lives in node_modules
import * as THREE from 'three';

console.log(THREE.REVISION)
